import React from "react";
import ReactDOM from "react-dom";
import { AppoPro } from "./ApolloProvider";

ReactDOM.render(
  <React.StrictMode>
    <AppoPro />
  </React.StrictMode>,
  document.getElementById("root")
);
